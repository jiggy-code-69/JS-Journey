const age = 18;
const isAdult = age >= 18 ? 'Yes' : 'No';
console.log(isAdult); // Output: Yes
The syntax of the ternary operator is:

condition ? value_if_true : value_if_false;

In this case, the condition is age >= 18, the value if true is 'Yes', and the value if false is 'No'. If the condition is true, the value if true is returned, otherwise the value if false is returned.
